# 直播项目

## 开场白

各位屏幕前的开发者朋友们，大家晚上好！欢迎来到本次线上技术直播，我是今天的主讲人 胡靖。首先非常感谢大家在忙碌的工作和学习中抽出时间参与这场直播，无论是刚进入直播间的同学，还是提前预约守候的老朋友，都欢迎大家。也麻烦大家动动手，给直播间点个赞、加个关注，后续我们还会持续分享更多实用的 AI 应用开发内容。

今天这场直播的主题是 **《TinyRobot 助你一站式搞定 AI 界面开发》**。相信很多同学在做 AI 应用时都会遇到类似问题：一开始觉得只是做一个聊天框，但真正落到产品里，就会发现还需要消息流、输入区、生成状态、错误重试、复制、移动端适配等一整套界面和交互细节。组件零散拼装不仅耗时间，也很容易造成体验不一致。

所以今天我们会围绕 TinyRobot 组件库，带大家看一看如何更高效地搭建一个高质量 AI 应用界面。从组件库定位、完整成品展示，到核心组件组合实践，再到不同 AI 场景下的产品化扩展，帮助大家理解 TinyRobot 不只是提供单个组件，而是面向 AI 应用界面的一套基础设施。

## 目录

简单跟大家介绍一下今天的直播流程：首先我会用几分钟时间介绍 TinyRobot 的定位和它解决的问题；接着展示一个完整的 AI 助手界面，让大家先看到最终效果；然后进入实操环节，从零搭建一个 AI 助手页面，包括页面布局、消息组件、输入组件，以及加载、重试、复制等常见状态；最后我们会看一下如何把同一套组件扩展到客服助手、知识库问答、代码助手等更多 AI 应用场景中。

直播过程中大家有任何疑问，都可以随时在弹幕里留言，我会在最后预留时间集中解答大家的问题。过程中也会有一些小互动和福利，我们会针对优秀提问的开发者朋友送出周边礼品，大家可以全程跟上节奏，不要错过。

话不多说，我们马上进入今天的主题。先从 **TinyRobot 的定位：面向 AI 应用的界面基础设施** 开始，看看为什么 AI 界面开发不只是做一个聊天框，以及 TinyRobot 能帮我们解决哪些实际问题。

## 过渡页

现在，让我们进入第一部分，先聊一聊 TinyRobot 的定位。

## demo和产品对比

很多同学在刚开始做 AI 应用的时候，第一反应往往是：我只需要做一个聊天框。左边这个示例，其实就是我们很常见的 Demo 形态，有一个消息列表，有一个输入框，再加一个发送按钮。它可以完成最基础的对话能力，用来验证想法、跑通流程是完全没问题的。

但当这个能力要进入真实产品时，问题就会变得多起来。比如，AI 正在生成的时候，界面要怎么反馈？用户想停止生成怎么办？回答失败了是否可以重试？内容生成完之后，用户能不能复制、重新生成、继续追问？如果是客服场景，还可能需要快捷问题、转人工、业务状态提示。

也就是说，AI 应用界面真正复杂的地方，不是有没有一个输入框，而是围绕一次 AI 交互，如何把过程、结果和后续操作组织清楚。左边的 Demo 解决的是“能不能对话”，而右边的产品化界面要解决的是“用户能不能稳定、清楚、可控地完成任务”。

TinyRobot 的定位，就是把这些 AI 应用中高频出现的界面模块和交互模式沉淀下来。它不是只提供一个聊天框，而是帮助我们更快搭建消息流、输入区、生成状态、操作反馈，以及不同业务场景下需要的扩展内容。

## 从demo到产品

当我们完成一个基础 Demo 之后，通常会觉得 AI 应用已经跑起来了。但真正进入产品阶段，复杂度会很快上升。

第一个痛点是消息展示重复开发。几乎每个 AI 应用都需要用户消息、助手消息、长内容、Markdown、代码块这些展示能力。如果每个项目都重新实现一遍，不仅浪费时间，也很容易出现细节不一致。

第二个痛点是状态反馈处理成本高。AI 应用里有很多过程状态，比如生成中、停止生成、请求失败、重新生成、空状态等等。这些状态本身并不复杂，但如果每次都在业务页面里单独处理，就容易和业务逻辑耦合在一起，也会增加后续维护和复用成本。

第三个痛点是交互细节容易遗漏。像复制答案、重新生成、继续追问、错误提示、自动滚动这些功能，单独看都不大，但它们决定了一个 AI 界面到底是“能用”，还是“好用”。很多 Demo 到产品的差距，其实就藏在这些细节里。

第四个痛点是多场景复用成本高。在不同 AI 应用场景中，比如客服助手、知识库问答、代码助手，业务表达会有差异，但基础界面结构往往相似，都需要消息流、输入区、状态反馈和操作区。如果缺少统一的组件基础，场景扩展时就容易产生重复开发成本。

AI 界面开发真正耗时的地方，往往不是第一个 Demo，而是从 Demo 走向产品时，需要补齐的大量状态、细节和复用能力。TinyRobot 要解决的，也正是这部分高频、重复、容易被低估的工作。

## 定位和核心价值

前面我们提到，AI 应用从 Demo 走向产品，会遇到很多重复建设和体验不一致的问题。TinyRobot 的定位，就是面向 AI 应用的界面基础设施。

这里的基础设施，不只是指一组 UI 组件，也包括支撑这些组件运行的数据管理能力。

所以当前可以把 TinyRobot 理解成两层：一层是组件库，一层是 Kit。

组件库主要负责 UI 组合。比如消息展示、输入与操作、状态与反馈，以及面向不同业务的场景化组合。它解决的是 AI 界面如何呈现、如何交互的问题，把 AI 应用中高频出现的界面模块沉淀成可复用组件。

Kit 主要负责数据管理。比如消息数据、会话数据、请求状态等。它解决的是组件背后的数据如何组织、状态如何流转，以及这些数据如何驱动界面更新的问题。

这两层结合起来，TinyRobot 就不只是一个静态组件库，而是可以同时覆盖界面呈现和数据驱动，帮助开发者更稳定地搭建 AI 应用界面。

所以 TinyRobot 的核心价值可以概括成三个关键词：完整性、一致性和扩展性。完整性，是覆盖 UI 展示和数据管理；一致性，是统一组件体验和数据流转方式；扩展性，是能够支撑更多 AI 应用场景，以及后续更高层的产品能力。

未来我们也会基于组件库和 Kit 继续沉淀 Chat 套件，让开发者不一定每次都从组件开始拼，而是可以通过更简单的配置，快速搭建出完整 AI 应用。当然，今天这场直播我们会先聚焦当前最核心的部分：组件库负责 UI 组合，Kit 负责数据管理。

## 产品展示

接下来我们进入第二部分，先看一个完整的客服助手界面应该包含哪些部分。

相比前面看到的普通聊天框，客服场景下的 AI 助手界面会更接近真实业务。首先左侧会有历史对话区域，用户可以新建对话，也可以回到之前的对话记录里继续处理问题。对于客服场景来说，用户的问题往往不是一次问答就结束，历史会话能帮助用户保留上下文。

中间是核心的消息流区域，这里承载用户问题和客服助手的回复。客服回复通常不只是简单一句话，而是需要分步骤说明处理方式、给出建议，或者明确下一步操作，所以消息展示需要有清晰的结构。

底部是输入与操作区域。它不只是一个输入框，还会承载发送、停止生成、快捷入口等操作。用户从这里发起咨询，也通过这里继续补充问题和控制对话过程。

在整个交互过程中，还需要状态与反馈。比如客服助手是否正在生成回复，是否正在查询相关信息，或者请求失败后能不能重试。这些状态能让用户知道当前服务是否还在处理，而不是面对一个没有反馈的界面。

最后是推荐追问。客服场景里，用户完成一次咨询后，通常还会有一些相关的后续问题，所以在回复之后提供追问建议，可以帮助用户更顺畅地继续咨询，比如“如何申请退款”“多久可以到账”“是否可以转人工”等。这类推荐追问能让客服助手不只是被动回答，而是进一步引导用户完成问题处理。

也就是说，客服助手不是把聊天框做漂亮就够了，更重要的是把会话、输入、状态和后续引导 组织成完整体验。TinyRobot 要做的，就是把这些通用能力沉淀成可以复用的组件。

## 功能展示

**切到网页后的过渡句**

> 刚才 PPT 里我们已经看过这个客服助手界面的组成，接下来不再静态介绍结构，我们直接模拟一次真实客服咨询，看这些模块怎么一起工作。
> 在正式演示之前先说明一下，为了保证直播节奏和演示稳定性，今天页面里的 AI 回复和部分状态流使用的是 mock 数据，并没有真实接入 LLM。我们这部分重点关注的是 TinyRobot 如何承载 AI 应用里的界面结构、交互流程和产品化细节。在真实项目中，这些回复内容可以替换成实际模型返回的数据。

然后直接进入操作。

**优化后的演示步骤**

1. **发起客服咨询**
   输入：

   > 我的订单已经付款了，为什么还没有发货？

   展示：
   - 输入框
   - 发送按钮
   - 用户消息进入消息流

2. **展示处理过程**
   展示：
   - 正在理解问题
   - 正在查询订单状态
   - 正在整理处理建议
   - 停止生成按钮

   重点讲：

   > AI 客服不能只显示一个转圈，用户需要知道系统正在处理什么。

3. **展示结构化回复**
   展示：
   - 分段回复
   - 可能原因
   - 建议操作
   - 消息操作：复制、重新生成、反馈

   重点讲：

   > 客服场景里，回答的目标不是简单“说一段话”，而是帮助用户更快理解问题并找到下一步处理方式。
   > 这里大家看到的回复结构，比如先给结论、再说明可能原因、最后给出建议操作，主要取决于真实项目里的大模型提示词设计。
   > TinyRobot 负责的是把这些内容稳定展示出来，并配合复制、重新生成、反馈等操作，让它更自然地出现在产品界面中。

4. **点击推荐追问**
   点击：

   > 预计什么时候发货？

   展示：
   - 推荐追问
   - 一键继续对话
   - 新问题进入消息流

   重点讲：

   > 推荐追问可以降低用户继续提问的成本，把一次回答延展成完整咨询流程。

5. **展示产品化操作**
   快速展示：
   - 复制回复
   - 重新生成
   - 新建咨询 / 切换历史会话

   重点讲：

   > 这些小操作决定了 AI 客服界面是否真的可用、顺手、可交付。

## 开始实践

接下来进入第三部分，我们会从零搭建一个 AI 助手页面。

这一部分会按照真实开发顺序来走。第一步是页面布局，先搭出整体页面容器，并划分出历史会话区、消息区和输入区。

第二步是消息区域，我们会接入消息列表，并区分用户消息和助手消息，让对话主流程先展示出来。

第三步是输入区域，接入输入框，让用户可以发送问题，同时也可以控制生成过程，比如发送和停止生成。

最后再补齐一些产品化交互，比如复制回复、重新生成、加载反馈和错误重试。这些细节会让页面从“能跑”更接近“可用”。

这页只是实操路线图。接下来我们直接切到代码，按这个顺序一步步搭出来。

## 实践 -- 页面布局

**第 1 步：搭建页面布局**

在正式接入消息组件之前，我们先从页面布局开始。

很多同学在做 AI 助手页面时，第一反应可能是先把聊天气泡和输入框做出来。但如果我们把几个真实的 AI 产品放在一起看，会发现布局本身其实也是一个很重要的基础能力。

常见的 AI 应用界面，通常会包含几个区域：顶部的标题和操作区，中间的主对话区域，底部的输入区，左侧的历史会话或导航区，有些产品还会在右侧放扩展面板，比如知识库引用、工具执行结果、上下文信息等。

再进一步看，不同产品的布局形态也会有差异。有的是完整工作台式布局，有的是全屏对话模式，有的是侧边吸附在业务系统里的助手面板。这些形态看起来不同，但底层其实都离不开区域划分、尺寸控制、滚动管理和响应式适配。

所以从组件库的角度来看，布局并不是简单写几个 `div`。它其实也可以沉淀成可复用的能力，帮助开发者快速搭出一个稳定的 AI 应用骨架。

TinyRobot 目前已经提供了 `Container` 容器组件，可以承载一部分页面结构能力。不过对于更完整的 AI 应用布局，比如头部、侧边栏、主内容区、输入区、扩展面板、多种布局模式切换，这块能力还可以继续完善。后续版本里，TinyRobot 也会进一步补充更完整的 `Layout` 相关组件，覆盖这些常见 AI 应用场景。

不过在今天的实操里，我们先不把布局做得太复杂。为了让大家更容易看清楚页面结构，我们会先用最基础的 CSS Grid 搭一个简单骨架：左侧是历史会话区，右侧分成顶部标题区、中间消息区和底部输入区。

这样做的好处是，大家可以先理解一个 AI 助手页面的基础区域划分。后面我们再逐步把 TinyRobot 的消息组件、输入组件、状态操作和历史会话能力接进来，整个页面就会从一个静态布局，慢慢变成一个可以使用的 AI 客服助手。

## 实践 -- 消息区域

第二步接入消息区域。

这里使用 `TrBubbleList` 渲染对话主流程，核心传入两个配置：`messages` 和 `roles`。

`messages` 是消息数据，决定列表里展示哪些内容，以及展示顺序。用户消息、助手消息，后续的工具调用消息，本质上都会进入这组数据中。原生支持 OpenAI兼容格式的 reasoning_content, tool_calls

`roles` 是角色配置，用来定义不同角色的展示方式。比如用户消息靠右，助手消息靠左，也可以配置头像、气泡形态等视觉信息。

所以这一部分可以理解为：`messages` 负责数据，`roles` 负责展示规则，`TrBubbleList` 负责把它们渲染成完整的消息流。

在演示里，我们先用 mock 数据接入消息列表，并区分用户和助手消息。这样页面的对话主流程就先建立起来了，后面再继续接入输入框和生成状态。

## 实践 -- 输入区域

第三步接入输入区域，同时把消息状态管理接进来。

这里会用到两个核心能力：Sender 和 useMessage。

Sender 负责输入侧交互，包括用户输入、发送，以及生成过程中的停止操作。

真正管理消息流和生成过程的是 useMessage。这里主要用到四个接口。

第一个是 messages，它是消息列表的数据源，会直接传给 BubbleList 渲染。

第二个是 isProcessing，表示当前是否处于生成中。这个状态可以传给 Sender，用于控制发送按钮和停止生成按钮的切换。

第三个是 sendMessage()，用户点击发送时调用它。它会把用户消息加入列表，并触发后续回复流程。

第四个是 abortRequest()，用于停止当前生成。这个能力在 AI 应用里很常见，尤其是流式回复较长时，用户需要随时中断。

所以这一节的关键不是单纯接一个输入框，而是把输入、消息数据和生成状态串起来：

Sender 产生用户操作，useMessage 管理消息和请求状态，BubbleList 消费 messages 完成展示。

完成这一步后，页面就从静态消息展示，变成了一个可以发送、生成、停止的 AI 对话界面。

## 实践补齐

最后一步补齐一些产品化交互。

前面三步完成之后，页面已经可以展示消息、发送问题、生成回复和停止生成。但一个真正可用的 AI 助手，还需要处理更多细节，比如复制回复、重新生成、加载反馈、错误重试，以及历史会话管理。

消息操作这部分，我们主要用 BubbleList 的 after 插槽来完成。after 插槽可以在气泡内容之后追加操作区，所以复制、重新生成、重试这类能力都可以放在这里扩展。这样做的好处是，不需要改气泡组件本身，只是在业务侧按场景补充操作。

错误处理这部分，使用 useMessage 的 plugin 机制。我们可以加一个 error 插件，在请求失败时统一修改消息状态，比如给最后一条助手消息打上 error 标记，再由 UI 决定是否展示重试入口。这样错误逻辑不会散落在组件里，而是收敛到消息流程里。

历史会话这部分，用到 History 组件和 useConversation。History 负责会话列表的展示和交互，比如新建、切换、编辑、删除。useConversation 负责管理多会话数据，每个会话都有自己的消息流，切换会话时页面展示对应的 messages。

这一节的重点是把页面从“能跑”往“可用”推进。
after 插槽解决消息级操作，plugin 机制解决流程级异常，useConversation 解决多会话管理。
这几个能力组合起来，AI 助手页面才更接近真实产品里的使用体验。

## 实践

- `pnpm create vite -t vue-ts chat-app`
- 清空style.css，写入基础styles，删除HelloWorld.vue，修改App.vue
- 安装依赖

  ```bash
  pnpm i
  pnpm i @opentiny/tiny-robot @opentiny/tiny-robot-kit @opentiny/tiny-robot-svgs
  ```

- layout布局
- 导入robot样式，接入消息列表，并区分用户消息和助手消息
- 接入输入框，让用户可以发送问题，同时也可以控制生成过程，比如发送和停止生成。接入kit
- 补齐一些产品化交互：复制回复、重新生成、错误重试
- 接入历史会话

## 扩展实践

最后一部分，我们来看产品化扩展。前面我们已经搭出了一个基础 AI 助手页面，也补齐了停止生成、重新生成、复制、错误重试这些常见交互。接下来我们不重复这些内容，而是在已有页面基础上继续增强，把它扩展成一个更接近真实产品的客服助手。

这一部分主要看三个增量。

第一是主题适配。客服助手可能会嵌入到不同产品里，有些产品是浅色界面，有些是深色工作台，所以我们会加上深浅色主题，让同一套界面可以适配不同视觉环境。

第二是响应式适配。真实产品不一定只在大屏里使用，也可能在较窄窗口或者移动端里打开。所以我们会调整页面在不同宽度下的表现，保证核心对话流程依然清晰可用。

第三是客服场景增强。客服场景里，用户经常不知道下一步该问什么，所以我们会加入推荐追问，比如“多久可以发货”“可以修改收货地址吗”“如何申请退款”等，让用户可以一键继续咨询，也让对话更容易向问题解决推进。

问题列表：

- 我的订单怎么还没发货？
- 我的退款什么时候到账？

## 第一步

扩展实践的第一步是主题适配。

这里主要用到两个能力：`TrThemeProvider` 和 `useTheme`。

`TrThemeProvider` 放在应用外层，负责提供主题上下文，统一管理亮色、深色，后续也可以扩展为跟随系统的 `auto` 模式。

在业务组件内部，可以通过 `useTheme` 直接获取主题状态和操作方法。比如用 `resolvedColorMode` 判断当前实际模式，用 `toggleColorMode` 完成亮暗切换。这样就不需要在组件之间层层透传主题状态。

业务样式部分，保持使用 TinyRobot 的主题变量即可。这样组件和自定义布局能跟随同一套主题体系，切换时整体视觉是一致的。

这一节重点展示的是：主题能力不只是单个组件变色，而是可以覆盖完整 AI 页面，包括布局、消息区、输入区和操作按钮。

## 第二步

这一节主要处理窄屏场景下的页面结构调整。

AI 助手页面在桌面端通常可以同时展示历史会话、消息流和输入区；但进入窄屏后，横向空间会明显收缩，如果继续保留桌面端结构，主对话区域会被压缩，消息阅读和输入体验都会受到影响。

具体做了两个调整。

第一，消息区在更小宽度下隐藏头像。头像在桌面端可以增强角色识别，但在手机宽度下会占用横向空间。隐藏头像后，消息内容可以获得更完整的展示宽度。

第二，历史会话从固定侧边栏切换为抽屉。桌面端保留左侧历史栏，方便快速切换；窄屏端则通过顶部按钮按需打开历史会话，避免它长期占用主对话空间。

## 第三步

第三步是客服场景增强。

前面我们已经有了基础的 AI 助手能力，包括消息展示、输入、生成状态、历史会话和响应式适配。接下来要做的是把它进一步贴近客服业务场景。

这里分成三个小步骤。

第一步是加入工具调用能力。

客服场景里，很多问题不能只靠模型直接回答。比如用户问订单为什么没发货、退款什么时候到账、物流到哪里了，这些都需要查询业务系统后再给出结果。

所以这里我们接入 toolPlugin，并用 mock 数据模拟工具调用流程。用户输入命中订单、退款、物流等关键词时，mock response 会返回对应的 tool_calls；随后由 mock callTool 返回查询结果；最后助手再基于工具结果生成客服回复。

这一步体现的是：AI 不只是生成文本，而是可以通过工具调用接入业务能力。

第二步是自定义工具调用的展示方式。

默认工具渲染更偏技术视角，适合展示工具名、参数、结果。但在客服场景里，用户不一定关心底层函数名和 JSON 参数，更关心的是“系统正在帮我查什么”“查询是否完成”。

所以这里用 TrBubbleProvider 的 content-renderer-matches 增加自定义渲染器，专门匹配带 tool_calls 的消息，把工具调用展示成更业务化的文案，比如“正在查询订单状态”“查询完成”。这样工具调用仍然存在，但呈现方式更符合客服产品的语境。

第三步是加上推荐追问。

客服助手不能只是回答完就结束。很多时候用户还需要继续追问，比如订单场景下可能会问“预计什么时候发货”“是否可以取消订单”“能不能修改收货地址”。

这里仍然使用 BubbleList 的 after 插槽，在助手回复后追加推荐追问。追问内容通过 mock 规则生成，点击后直接复用现有发送流程，相当于帮用户继续推进问题处理。

## 总结回顾

最后我们用一页简单回顾一下今天完成了什么。

一开始，我们先从定位出发，讨论了为什么 AI 应用界面不只是一个聊天框。真正产品化时，它会涉及消息、输入、状态、操作和场景内容，所以 TinyRobot 的定位是面向 AI 应用的界面基础设施。

接着我们看了一个完整客服助手界面，了解一个真实 AI 产品页面通常包含哪些部分，比如历史对话、消息流、输入区，以及客服场景里的推荐追问。

然后进入组件实践，从页面布局开始，逐步接入消息区域、输入区域和常见交互，把一个基础 AI 助手页面搭起来。

最后，我们在基础助手之上做了产品化扩展，包括深浅色主题、响应式适配，以及客服场景下的推荐追问，让页面更接近真实业务。

所以今天这条路线其实很清楚：从一个能对话的 Demo 出发，逐步补齐组件、交互和场景能力，最终走向一个更可交付的 AI 客服界面。这也是 TinyRobot 希望帮助开发者做到的事情。

> 学习文档、项目地址、demo代码地址
