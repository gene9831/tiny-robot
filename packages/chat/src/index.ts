import { ChatFooter, ChatHeader, ChatMain, ChatRoot, ChatSidebar } from './components'

export const Chat = {
  Root: ChatRoot,
  Header: ChatHeader,
  Main: ChatMain,
  Sidebar: ChatSidebar,
  Footer: ChatFooter,
}
