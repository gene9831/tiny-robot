import { ChatFooter, ChatHeader, ChatLayout, ChatMain, ChatRoot, ChatSidebar, ChatSidebarSwitch } from './components'

export const Chat = {
  Root: ChatRoot,
  Layout: ChatLayout,
  Header: ChatHeader,
  Main: ChatMain,
  Sidebar: ChatSidebar,
  Footer: ChatFooter,
  SidebarSwitch: ChatSidebarSwitch,
}
